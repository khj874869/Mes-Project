# ===== Ports =====
HTTP_PORT=80
POSTGRES_PORT=5432
KAFKA_HOST_PORT=29092
KAFKA_UI_PORT=8088

# ===== DB =====
POSTGRES_USER=mes
POSTGRES_PASSWORD=mes

# ===== Bootstrap admin for Operator UI =====
BOOTSTRAP_ADMIN_ENABLED=true
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=admin1234

# ===== Public HTTPS (Caddy Reverse Proxy) =====
# 외부 공개 시 사용: docker compose -f docker-compose.prod.yml -f docker-compose.public.yml up -d --build
# DOMAIN은 실제 도메인(A 레코드가 서버 IP를 가리키고, 80/443 포트가 열려 있어야 함)
DOMAIN=example.com
ACME_EMAIL=admin@example.com
PUBLIC_HTTP_PORT=80
PUBLIC_HTTPS_PORT=443
